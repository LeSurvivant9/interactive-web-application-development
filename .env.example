# Database configuration
POSTGRES_USER=user
POSTGRES_PASSWORD=password
POSTGRES_DB=mediatracker

# Backend configuration
JWT_SECRET="generate_a_random_secret_here"
TVDB_API_KEY="your_tvdb_api_key"
FRONTEND_URL="http://localhost:3001"

# Frontend configuration (needed for local development)
# Public API URL for browser-side calls
NEXT_PUBLIC_API_URL=http://localhost:3000/graphql
# Internal API URL for server-side calls (use http://backend:3000/graphql for Docker)
INTERNAL_API_URL=http://localhost:3000/graphql

# NextAuth configuration
AUTH_SECRET="generate_another_random_secret_here"
